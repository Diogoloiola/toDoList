<p id="notice"><%= notice %></p>

<h1>Atividades</h1>

<table>
  <thead>
    <tr>
      <th>Nomeatividade</th>
      <th>Conteudoatividade</th>
      <th>Criadapor</th>
      <th>Pessoaresponsavel</th>
      <th>Estado</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @atividades.each do |atividade| %>
      <tr>
        <td><%= atividade.nomeAtividade %></td>
        <td><%= atividade.conteudoAtividade %></td>
        <td><%= atividade.criadaPor %></td>
        <td><%= atividade.pessoaResponsavel %></td>
        <td><%= atividade.estado %></td>
        <td><%= link_to 'Show', atividade %></td>
        <td><%= link_to 'Edit', edit_atividade_path(atividade) %></td>
        <td><%= link_to 'Destroy', atividade, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Atividade', new_atividade_path %>
